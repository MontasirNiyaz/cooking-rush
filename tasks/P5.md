# P5 — Live-ops machine
Design: docs/COOKING_GAME_ROADMAP_M7-M12.md §M10.

## P5.1 Event/limited availability runner
**Steps:** Extend Restaurant schema with `availability {startUtc,endUtc}` +
`eventCurrencyId`; generic `EventService` shows/hides hub plots + level access by
server UTC; event currency as a profile wallet entry; expiry handling for unspent
currency (config policy).
**Accept:** fake-clock tests for appear/expire; an event restaurant defined purely in
config appears and expires correctly; no event-specific Luau.

## P5.2 Season pass
**Steps:** `Seasons.lua` (free+premium tracks, tier XP from play events, reward grants
via existing services); season rollover logic (pure+tested); pass UI; premium unlock
via gamepass/dev product reusing P3.6 pipeline.
**Accept:** tier-up grants idempotent; rollover archives progress; premium purchase
mid-season back-grants earned premium tiers.

## P5.3 Leaderboards
**Steps:** `LeaderboardService` on OrderedDataStores: per-restaurant high score,
prestige level, collection %; global + friends views; hub leaderboard board model.
**Accept:** write throttling within DataStore budgets; displays update on schedule;
no client-writable scores.

## P5.4 Restaurant #3 (the recurring M6 test)
**Steps:** Author a third restaurant ENTIRELY as config + assets (content doc pattern:
ingredients/recipes/stations reuse archetypes; curve constants; overrides for tutorial-
free intro + boss). 
**Accept:** zero engine diffs in the PR. If an engine change is required, STOP — log it
in ROADMAP Decisions; fix the engine generically in a separate task first.
